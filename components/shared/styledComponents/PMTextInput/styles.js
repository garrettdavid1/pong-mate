import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    default: {
        fontSize: 20,
        textAlign: 'center',
        height: 50,
        width: '80%',
        backgroundColor: '#fff',
        marginBottom: 20,
        borderWidth: 1
    }
})